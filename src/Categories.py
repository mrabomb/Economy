import Item

class DiscontinuedItems(Item):
    def __init__(self, rawData):
        self.rawData = rawData

    def parse(self, rawData):
        '''parse the data and put it where it belongs'''
        return parsedData

    def lowers(self, parsedData):
        '''put the lowers info where it belongs in the Item object'''

    def edibles(self, parsedData):
        '''put the edibles info where it belongs in the Item object'''

    def phats(self, parsedData):
        '''put the phats info where it belongs in the Item object'''

    
class Weapons(Item):
    def __init__(self, rawData):
        self.rawData = rawData

    def parse(self, rawData):
        '''parse the data and put it where it belongs'''
        return parsedData

    def drygores(self, parsedData):
        '''put the drygores info where it belongs in the Item object'''

    def ascensions(self, parsedData):
        '''put the ascensions info where it belongs in the Item object'''

    def seismics(self, parsedData):
        '''put the seismics info where it belongs in the Item object'''

    def noxious(self, parsedData):
        '''put the noxious info where it belongs in the Item object'''

    def telos(self, parsedData):
        '''put the telos info where it belongs in the Item object'''

    def aod(self, parsedData):
        '''put the aod info where it belongs in the Item object'''

    def khopesh(self, parsedData):
        '''put the aod info where it belongs in the Item object'''

        
class Codex(Item):
    def __init__(self, rawData):
        self.rawData = rawData

    def parse(self, rawData):
        '''parse the data and put it where it belongs'''
        return parsedData

    def codex(self, parsedData):
        '''put the codex info where it belongs in the Item object'''

        
class TreasureTrails(Item):
    def __init__(self, rawData):
        self.rawData = rawData

    def parse(self, rawData):
        '''parse the data and put it where it belongs'''
        return parsedData

    def dyes(self, parsedData):
        '''put the dyes info where it belongs in the Item object'''

    def cases(self, parsedData):
        '''put the cases info where it belongs in the Item object'''

    def capes(self, parsedData):
        '''put the capes info where it belongs in the Item object'''
    
class SoF(Item):
    def __init__(self, rawData):
        self.rawData = rawData

    def parse(self, rawData):
        '''parse the data and put it where it belongs'''
        return parsedData

    def misc(self, parsedData):
        '''put the misc info where it belongs in the Item object'''

    def seasons(self, parsedData):
        '''put the seasons info where it belongs in the Item object'''

    def mimic(self, parsedData):
        '''put the mimic info where it belongs in the Item object'''

    def pinata(self, parsedData):
        '''put the pinata info where it belongs in the Item object'''

    def prize(self, parsedData):
        '''put the prize info where it belongs in the Item object'''

    def gem(self, parsedData):
        '''put the misc info where it belongs in the Item object'''

    def beach(self, parsedData):
        '''put the beach info where it belongs in the Item object'''


class ThirdAge(Item):
    def __init__(self, rawData):
        self.rawData = rawData

    def parse(self, rawData):
        '''parse the data and put it where it belongs'''
        return parsedData

    def melee(self, parsedData):
        '''put the melee info where it belongs in the Item object'''

    def range(self, parsedData):
        '''put the range info where it belongs in the Item object'''

    def mage(self, parsedData):
        '''put the mage info where it belongs in the Item object'''

    def druidic(self, parsedData):
        '''put the druicid info where it belongs in the Item object'''

        
class Gear(Item):
    def __init__(self, rawData):
        self.rawData = rawData

    def parse(self, rawData):
        '''parse the data and put it where it belongs'''
        return parsedData

    def shield(self, parsedData):
        '''put the shield info where it belongs in the Item object'''

    def tectonic(self, parsedData):
        '''put the tectonic info where it belongs in the Item object'''

    def sirenic(self, parsedData):
        '''put the sirenic info where it belongs in the Item object'''

    def malevolent(self, parsedData):
        '''put the malevolent info where it belongs in the Item object'''

    def gloves(self, parsedData):
        '''put the gloves info where it belongs in the Item object'''

    def boots(self, parsedData):
        '''put the boots info where it belongs in the Item object'''

    def jewelry(self, parsedData):
        '''put the jewelry info where it belongs in the Item object'''
